#include <stdio.h>

int main(void)
{
    int n;
    char winner[10];
    
    printf("Number of voters: ");
    scanf("%i", &n);
    
    char candidates[n][10];

    for(int i=0; i<n; i++)
    {
        
        printf("vote: ");
        scanf("%s", candidates[i]);
    }
    //[qwe,asd,zxc,asd]
    int winning;
    
    for(int j=0; j<n; j++)
    {
        for(int k=0; k<n && k !=j; j++)
        {
            if(candidates[j] == candidates[k])
                winning ++;
        }
        //i still want to store the times of each winner
        //then choose the most Number
        //then print them
    }   
}
